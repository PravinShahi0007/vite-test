import { Outlet } from "react-router-dom"

const AccountLayout = () => {
    return(
        <div>
            Account Layout
            <Outlet/>
        </div>

    )
}

export default AccountLayout